apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:

  # annotations:
  #   olm.providedAPIs: Central.v1alpha1.platform.stackrox.io,SecuredCluster.v1alpha1.platform.stackrox.io,SecurityPolicy.v1alpha1.config.stackrox.io

  generateName: rhacs-operator-

  name: rhacs-operator
  namespace: rhacs-operator

spec:
  upgradeStrategy: Default
